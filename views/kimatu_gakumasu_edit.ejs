<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>編集 - <%= data.name %>
    </title>
    <link rel="stylesheet" href="/kimatu_saki.css">
</head>

<body class="blue-theme">
    <div class="container">
        <header class="page-header">
            <h2>
                <%= data.name %> を編集
            </h2>
        </header>
        <form method="post" action="/kimatu_gakumasu_list/update/<%= data.id %>">
            <table>
                <tr>
                    <td>名前</td>
                    <td><input type="text" name="name" value="<%= data.name %>"></td>
                </tr>
                <tr>
                    <td>コスト</td>
                    <td><input type="number" name="cost" value="<%= data.cost %>"></td>
                </tr>
                <tr>
                    <td>コスト種別</td>
                    <td>
                        <select name="cost_type">
                            <option value="ノーマル" <%=data.cost_type==='ノーマル' ? 'selected' : '' %>>ノーマル</option>
                            <option value="やる気" <%=data.cost_type==='やる気' ? 'selected' : '' %>>やる気</option>
                            <option value="好印象" <%=data.cost_type==='好印象' ? 'selected' : '' %>>好印象</option>
                        </select>
                    </td>
                </tr>
                <tr>
                    <td>レアリティ</td>
                    <td><select name="rarity">
                            <option value="N" <%=data.rarity==='N' ? 'selected' : '' %>>N</option>
                            <option value="R" <%=data.rarity==='R' ? 'selected' : '' %>>R</option>
                            <option value="SR" <%=data.rarity==='SR' ? 'selected' : '' %>>SR</option>
                            <option value="SSR" <%=data.rarity==='SSR' ? 'selected' : '' %>>SSR</option>
                        </select></td>
                </tr>
                <tr>
                    <td>解禁レベル</td>
                    <td><input type="number" name="unlock_level" value="<%= data.unlock_level %>"></td>
                </tr>
                <tr>
                    <td>効果</td>
                    <td><textarea name="effect"><%= data.effect %></textarea></td>
                </tr>
            </table>
            <button class="btn" type="submit">保存</button>
            <a class="btn" href="/kimatu_gakumasu_detail/<%= data.id %>">キャンセル</a>
        </form>
    </div>
</body>

</html>